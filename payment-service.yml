server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/micro_services2
    username: root
    password: Prabhanshu

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
